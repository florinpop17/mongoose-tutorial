extends layout

block content
    .container
        .row
            .col-md-6.col-md-offset-3
                h1 Add New User
                form(action="/addUser")
                    .form-group
                        label(for="name") Name
                        input(class="form-control", name="name", type="text", placeholder="Name")
                    .form-group
                        label(for="email") Email
                        input(class="form-control", name="email", type="email", placeholder="Email")
                    button(type="submit", class="btn btn-primary btn-block") Add User