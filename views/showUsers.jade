extends layout

block content
    div.container
    h1= sub-title
    ul.userlist
        each user, i in users
            li
                a(href="mailto:#{user.email}")= user.username
                span - #{user.email}
